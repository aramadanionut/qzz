import React from "react";

import './Login.scss';

export default function Login(props) {
    return (
        <div className="Login">
            Login
        </div>
    );
}

Login.propTypes = {
};

Login.defaultProps = {
};
