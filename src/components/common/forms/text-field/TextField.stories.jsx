import React from 'react';

import { SIZES } from 'utils/constants'

import TextField from './TextField';

export default {
  component: TextField,
  title: 'Components/TextField',
}

export const Primary = (args) => <TextField></TextField>;

Primary.args = {
};